export type PlanOption = {
    id: string;
    label: string;
    priceMonthly: number;
    priceYearly: number;
    icon: string;
};

export type AddonOption = {
    id: string;
    title: string;
    description: string;
    priceMonth: number;
    priceYear: number;
};
